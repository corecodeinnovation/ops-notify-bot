services:
  bot:
    build: .
    env_file: .env
    ports: ["3001:3001"]
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3001/health"]
      interval: 30s
